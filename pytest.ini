[pytest]
asyncio_mode = auto
# One event loop for the whole session so asyncpg pool stays valid across tests.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    integration: integration tests (handler + db)
